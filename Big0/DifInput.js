function DifinputType (array,array2) {
for (let index = 0; index < array.length; index++) {
    const element = array[index]; //O(n) n = input array
    
}

for(let i=0; i < array2.length; i++){
    console.log(array2[i]); //O(m) m = input array2
}

}

// so the BigO would be O(n)+O(m)  = O(n+m) 
// So based on the input type the BigO notation would change